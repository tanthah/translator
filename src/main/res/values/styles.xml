<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Theme.Translator.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Spinner themes for dark backgrounds -->
    <style name="SpinnerTheme.Dark" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@drawable/bg_rounded_white</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:padding">8dp</item>
    </style>

    <!-- Button styles -->
    <style name="CameraButton" parent="Widget.Material3.Button">
        <item name="android:layout_margin">8dp</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="backgroundTint">@color/primary_color</item>
    </style>

    <!-- Card styles -->
    <style name="TranslationCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Text styles for accessibility -->
    <style name="LargeText">
        <item name="android:textSize">18sp</item>
        <item name="android:lineSpacingExtra">4dp</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="MediumText">
        <item name="android:textSize">16sp</item>
        <item name="android:lineSpacingExtra">2dp</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="SmallText">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_secondary</item>
    </style>
</resources>